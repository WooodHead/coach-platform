{
  "name": "hasura",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "console": "hasura --envfile ../variables.env console --no-browser",
    "console-prod": "hasura --envfile ../variables.env console --endpoint https://coach.scbirs.ch/hasura --no-browser"
  },
  "author": "Tim (https://tiim.ch/)",
  "license": "MIT",
  "devDependencies": {
    "hasura-cli": "^1.3.3"
  }
}